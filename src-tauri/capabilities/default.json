{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissões da janela principal. A UI só fala com o core via comandos próprios; nenhum plugin é exposto direto ao webview.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
